# Decisions

Why this repository is shaped the way it is. One file per decision, numbered, never edited after acceptance; a change
of mind is a new record that supersedes the old one. The README says what is where; `bootstrap.sh` says how; these say why.
