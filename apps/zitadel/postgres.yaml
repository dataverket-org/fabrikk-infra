apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: zitadel-db
  namespace: zitadel
spec:
  instances: 3
  storage:
    size: 32Gi
  enableSuperuserAccess: true
  bootstrap:
    initdb:
      database: zitadel
      owner: zitadel
