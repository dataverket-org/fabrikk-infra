# The Barman Cloud plugin for CloudNativePG (CNPG-I): backups and WAL archiving to an object store through an
# ObjectStore resource. The in-tree barmanObjectStore is deprecated. Same namespace as the operator (cnpg-system),
# needs cert-manager for its own server and client certificates. Pinned to a release manifest like cnpg-operator.
resources:
  - https://github.com/cloudnative-pg/plugin-barman-cloud/releases/download/v0.15.0/manifest.yaml
